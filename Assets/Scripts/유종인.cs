using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class 유종인 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("a");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
